[project]
name = "homeassistant-an24net"
version = "0.1.0"
requires-python = ">=3.14.2"
dependencies = []

[dependency-groups]
dev = [
    "homeassistant>=2026.4.2",
]

[tool.pyright]
include = ["custom_components/intelbras"]
typeCheckingMode = "strict"
